"""Feasibility agent placeholder."""

class FeasibilityAgent:
    def __init__(self, name: str = "feasibility-agent"):
        self.name = name

    def assess(self, plan):
        """Assess feasibility of a plan (placeholder)."""
        return {"feasible": True}
